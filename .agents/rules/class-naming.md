# Class Naming and File Structure Rules

## File Organization for Classes
- Each class MUST be defined in its own file.
- The file name MUST match the class name exactly (e.g., class `BaseStorageOperation` should be in `BaseStorageOperation.ts`).
- Avoid bundling multiple classes or unrelated logic in a single file unless they are small private helper classes.
